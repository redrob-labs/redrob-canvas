# Safe bounded generic-format boundaries across FFI and Qt

The second-pass fixes close both native boundary defects from the prior review. Generic FFI export now rejects aliased output destinations before creating Rust references, resets the shared destination, and documents the ownership contract. Qt project and interchange loading now cap reads from the opened handle at 64 MiB plus one byte, with an oversized sparse-file regression proving rejection before editor replacement. Focused FFI tests, strict Clippy, and the native smoke test pass.

**Watch for:** No blocking or non-blocking issues remain.

**Verdict**: APPROVED

## High-level view

The export ABI establishes destination distinctness before turning either pointer into a mutable reference. Equal non-null destinations return `REDROB_ERROR`, leave the shared slot at `{NULL,0}`, and are covered through the public ABI.

Project open and interchange import now enforce the input ceiling against bytes read from the opened `QFile`, not path metadata. Over-limit input returns before document replacement, preserving project identity and editor state.

<details>
<summary>Issues (0)</summary>

No blocking or non-blocking issues remain.

</details>

<details><summary>Details</summary>

## Native boundary fixes close both prior failure paths

`reset_two_buffers` compares raw destinations before constructing either `&mut RedrobBuffer`; equality resets the shared slot once and fails, while distinct destinations retain the existing reset-before-failure contract. The public header requires distinct objects, and the regression verifies the error, reset buffer, and diagnostic through `redrob_editor_export_file`.

`readBoundedFormatFile` requests 64 MiB plus one byte from the opened handle and rejects the sentinel extra byte before either caller reaches `replaceFromGenericBytes`. Native smoke exercises a sparse `64 MiB + 1` PNG and verifies unchanged project path, frame, and generation; the focused 18-test FFI suite, strict Clippy, and Qt 6.12 `redrob_native_smoke` all pass.

</details>

<details>
<summary>File map</summary>

- `crates/redrob-ffi/src/abi.rs`, `crates/redrob-ffi/include/redrob_ffi.h` — distinct-output enforcement and documented export contract.
- `crates/redrob-ffi/tests/ffi_api.rs` — aliased-output regression.
- `native/qt/EditorBridge.cpp` — bounded open-handle reads.
- `native/qt/main.cpp` — sparse oversized-input state-preservation smoke.

Full diff: unavailable because `feat/initial-editor` has no commits and all repository files are untracked; this pass compared the filesystem with `semantic-review/2026-09-08-060821-pr-3.md`.

</details>
